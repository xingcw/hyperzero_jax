# JAX for Google Cloud TPU
# On TPU VMs, install with libtpu index:
#   pip install -r requirements_tpu.txt -f https://storage.googleapis.com/jax-releases/libtpu_releases.html
# Or with uv:
#   uv pip install -r requirements_tpu.txt -f https://storage.googleapis.com/jax-releases/libtpu_releases.html
jax[tpu]>=0.4.31
jaxlib>=0.4.31
flax>=0.8.5
optax>=0.2.2
orbax-checkpoint>=0.5.15
chex>=0.1.86
numpy>=1.24.1
hydra-core==1.2.0
hydra-submitit-launcher==1.1.5
pandas>=1.3.0
matplotlib
wandb
termcolor
torch
GitPython
pytest
mujoco-warp
# Optional: pip install brax (pulls in mujoco; omit for TPU-only)
